import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

const searchSlice = createSlice({ name: 'search', initialState, reducers: {}, extraReducers: {} });

export default searchSlice.reducer;
